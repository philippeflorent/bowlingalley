export class Constants
{
	/* to adress points array (cfr BowlingFrameData) */
	static readonly THROW_A 	= 0; // first throw points
	static readonly THROW_B 	= 1; // second throw points
	static readonly BONUS		= 2; // bonus points
	// here you can specify how many frames in one game
	static readonly MAX_FRAMES  = 10;
	// here you can specify if debug fdata is created
	static readonly DEBUG_DATA  = false;
}